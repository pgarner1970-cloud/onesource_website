

# Live admin data and uploads - do not overwrite via Git deploy
/data/*.json
/admin/data/*.json
/uploads/
# Runtime admin data - edited live by the website/admin suite
# Legacy data location no longer used
